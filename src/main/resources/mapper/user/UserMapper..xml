<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitri.weatherwear.MyBatis.UserMapper">
    <select id="findAll" resultType="User">
        SELECT * FROM USERDATA
    </select>

    <insert id="save" parameterType="User">
        INSERT INTO USERDATA(user_id, user_pwd, user_name, user_loc_latitude, user_loc_longitude)
        VALUES (#{id},#{pwd},#{name},#{loc_latitude},#{loc_longitude})
    </insert>

    <select id="findOne" resultType="User">
        Select * from USERDATA where user_id = #{id}
    </select>

    <delete id="deleteById" parameterType="User">
        DELETE FROM USERDATA WHERE user_id = #{id}
    </delete>

    <update id="changeLocationById" parameterType="User">
        UPDATE USERDATA SET USER_LOC_LATITUDE = #{loc_latitude}, USER_LOC_LONGITUDE = #{loc_longitude} where USER_ID = #{id}
    </update>
</mapper>